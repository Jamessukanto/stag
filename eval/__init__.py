"""eval/ module - owned by the Evaluation chat.

Loads ModelArtifact files from disk and computes reciprocal metrics.
May import from src.core only. Must NEVER import from models/ (hard boundary).
"""
