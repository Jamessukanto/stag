"""Reusable, model-agnostic data mechanics (the "how").

Each function takes explicit inputs and returns structured outputs. Domain
orchestration (the "why/when") lives in ``data.loader``, not here.
"""
